$("#social").bind('pageinit', function() {

        /*
    if(localStorage.hasItem("facebook_accessToken")){
        alert('ok')
        var token = window.localStorage.getItem('facebook_accessToken');

        if(token === null){
    
            $('#facebook_login').click(acebook.onFacebookLogin);
            
        }else{
            
            $('#facebook_login').click(facebook.onFacebookLogout);
            
        }
        */
    }
    
});